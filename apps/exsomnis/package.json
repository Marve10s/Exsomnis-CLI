{
  "name": "exsomnis",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/bin.ts",
    "compile": "bun build --compile --minify --sourcemap ./src/bin.ts --outfile ../../dist/exsomnis"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "catalog:",
    "@effect/platform-bun": "catalog:",
    "@effect/sql-sqlite-bun": "catalog:",
    "@exsomnis/core": "workspace:*",
    "@tanstack/hotkeys": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:"
  }
}
